package com.example.leetcode.common;

// import java.util.Comparator;
// import java.util.List;

public class Util {
  // https://stackoverflow.com/q/35761864
  // public static final Comparator<T extends Comparable<T>> listComparator = (list1, list2) -> {
  //   // TODO: Handle null lists
  //   //
  //   // If the lists are long linked lists, accessing by index is inefficient.  In this
  //   // case one should prefer to use two iterators.
  //   for (int i = 0; i < Math.min(list1.size(), list2.size()); i++) {
  //     int diff = list1.get(i).compareTo(list2.get(i));
  //     if (diff != 0) {
  //       return diff;
  //     }
  //   }
  //   return list1.size() - list2.size();
  // };

  // public static final Comparator<? super T[]> arrayComparator = (T[] array1, T[] array2) -> {
  //   for (int i = 0; i < Math.min(array1.length, array2.length); i++) {
  //     if (array1[i].compareTo(array2[i]) != 0) {
  //       return array1[i].compareTo(array2[i]);
  //     }
  //   }
  //   return array1.length - array2.length;
  // };
}
